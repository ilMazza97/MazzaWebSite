using System.Data.Entity.ModelConfiguration;

namespace MazzaWebSite.Models
{
    public class CommissionPercentageConfiguration : EntityTypeConfiguration<CommissionPercentage>
    {
        public CommissionPercentageConfiguration()
        {
            //TODO
            /*builder.HasMany(e => e.UserAccountEntity)
                .WithOne(e => e. )
                .HasForeignKey(e => e.TelegramAccountId);*/
        }
    }
}
